export type TCooperationMail = {
  firstName: string;
  company: string;
  type: string;
  email: string;
  phone?: string;
};

export type TContactMail = {
  firstName: string;
  email: string;
  text: string;
};
